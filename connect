#!/usr/bin/env bash
port=$1
old_stty=$(stty -g)
rows=$(tput lines)
cols=$(tput cols)
echo "Current Local Window: ${rows}x${cols}"
stty raw -echo

for i in {1..2}; do
socat file:`tty`,raw,echo=0 tcp-listen:$port,reuseaddr
sleep 1
done

stty "$old_stty"
reset
